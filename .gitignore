# composer - installed by update-dependencies
build/composer.phar

# package
build/*.zip
build/.directory

# vendor - required only for update process
vendor

# composer.lock is not required because we commit our classes into git
composer.lock
